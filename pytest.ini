[pytest]
; The pure-logic tests run without Home Assistant. Globally installed HA test
; plugins (pytest-homeassistant-custom-component, pytest-socket) inject
; fixtures that error outside an HA test harness, so keep them out.
addopts = -p no:homeassistant -p no:socket -p no:sugar
testpaths = tests
